{
  "id": "${plugin_id}",
  "name": "${plugin_name}",
  "version": "${version}",
  "main": "${group}.VelocityPlugin",
  "description": "${description}",
  "authors": [
    "${plugin_author}"
  ],
  "url": "${homepage_url}"
}
